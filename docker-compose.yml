services:
  tigo:
    build: .
    image: tigo-mmu-influx:latest
    container_name: tigo
    restart: unless-stopped
    env_file:
      - tigo.env
    ports:
      - "8088:8088"
    volumes:
      - tigo_state:/opt/tigo

volumes:
  tigo_state:
